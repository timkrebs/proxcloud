---
description: Create an Architecture Decision Record
---
Delegate to the **architect** agent to write an ADR for: $ARGUMENTS

The ADR goes in docs/adr/ with the next sequential number, using the format Context → Decision → Consequences → Alternatives considered. Have the architect check existing ADRs first so decisions stay consistent, and return the path of the new file plus a three-sentence summary of the decision.
