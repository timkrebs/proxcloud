root = "."
tmp_dir = "tmp"

[build]
bin = "./tmp/proxcloud"
cmd = "go build -o ./tmp/proxcloud ./cmd/proxcloud"
delay = 200
exclude_dir = ["tmp", "bin"]
include_ext = ["go"]
stop_on_error = true

[log]
main_only = true
